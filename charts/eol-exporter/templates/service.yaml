apiVersion: v1
kind: Service
metadata:
  name: {{ include "eol-exporter.fullname" . }}
  labels:
    {{- include "eol-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "eol-exporter.selectorLabels" . | nindent 4 }}
